<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Sai Products — Shop</title>
  <meta name="description" content="Sai Products: Fresh batters, podi, oils & coconuts. Order online from Sahidya, Morais City, Trichy." />
  <style>
    :root{
      --pink:#FFD6E0; /* background */
      --cream:#FFF7E1; /* cards */
      --brown:#8B5E3C; /* headings */
      --button:#8D6E63; /* buttons */
      --text:#4E342E;
      --accent:#E8C07D;
      font-family: 'Poppins', Arial, sans-serif;
    }
    html,body{height:100%;margin:0;background:var(--pink);color:var(--text)}
    .app{max-width:980px;margin:20px auto;padding:18px}
    header{display:flex;align-items:center;gap:12px}
    .logo{background:var(--cream);border-radius:12px;padding:10px 14px;box-shadow:0 3px 10px rgba(0,0,0,0.06)}
    h1{margin:0;color:var(--brown);font-size:22px}
    p.lead{margin:6px 0 12px;color:var(--text)}
    .grid{display:grid;grid-template-columns:1fr 320px;gap:18px}
    .card{background:var(--cream);border-radius:12px;padding:12px;box-shadow:0 6px 18px rgba(0,0,0,0.06)}
    .product{display:flex;gap:12px;align-items:center;padding:10px;border-radius:10px;border:1px solid rgba(0,0,0,0.03);margin-bottom:10px}
    .img{width:72px;height:72px;background:linear-gradient(180deg,#fff,#f7e8e8);border-radius:10px;display:flex;align-items:center;justify-content:center;font-weight:700;color:var(--brown)}
    .meta{flex:1}
    .meta h3{margin:0;font-size:16px;color:var(--brown)}
    .meta p{margin:4px 0;color:#6b4b3b;font-size:13px}
    .controls{display:flex;flex-direction:column;gap:6px;align-items:flex-end}
    button{background:var(--button);color:#fff;border:none;padding:8px 12px;border-radius:8px;cursor:pointer}
    .small{background:transparent;color:var(--button);border:1px solid rgba(0,0,0,0.06);padding:6px 8px}
    aside .card{position:sticky;top:18px}
    .cart-list{max-height:300px;overflow:auto}
    .cart-item{display:flex;justify-content:space-between;margin-bottom:8px}
    .muted{font-size:13px;color:#6b4b3b}
    .total{font-weight:700;font-size:18px;color:var(--brown)}
    footer{margin-top:18px;text-align:center;color:#5a4036}
    .tag{display:inline-block;background:var(--accent);padding:4px 8px;border-radius:8px;color:#3b2416;font-weight:600}
    .success{background:linear-gradient(90deg,#dff7ea,#e4fff3);border:1px solid #c3f0d9;padding:10px;border-radius:8px;color:#1e6b3a}
    input[type="number"]{width:58px;padding:6px;border-radius:6px;border:1px solid #e8d9d3}
    .note{font-size:13px;color:#6b4b3b}
  </style>
</head>
<body>
  <div class="app">
    <header>
      <div class="logo">
        <h1>Sai Products</h1>
        <div class="tag">Fresh • Homemade</div>
      </div>
      <div style="margin-left:auto;text-align:right">
        <div class="muted">Owner: Sahidya</div>
        <div class="muted">Morais City, 6th Sector, Trichy</div>
      </div>
    </header>

    <p class="lead">Fresh batters, podis, pure oils & coconuts — made with love. Click items to add to cart. Packaging ₹15 added automatically.</p>

    <div class="grid">
      <main>
        <div class="card">
          <h2 style="margin-top:0;color:var(--brown)">Products</h2>

          <div id="products"></div>

        </div>

        <div style="height:14px"></div>
        <div class="card">
          <h3 style="margin:0;color:var(--brown)">About</h3>
          <p class="muted">Sai Products by Sahidya — fresh, no preservatives, sealed with safety. Contact: 8072100476 • sahidyack04@gmail.com</p>
        </div>
      </main>

      <aside>
        <div class="card">
          <h3 style="margin:0;color:var(--brown)">Cart</h3>
          <div class="cart-list" id="cartList" style="margin-top:8px"></div>

          <hr style="border:none;border-top:1px solid rgba(0,0,0,0.06);margin:12px 0">
          <div class="muted">Packaging: ₹15 (auto)</div>
          <div style="display:flex;justify-content:space-between;align-items:center;margin-top:8px">
            <div class="muted">Total:</div>
            <div class="total" id="totalPrice">₹0</div>
          </div>

          <div style="margin-top:12px;display:flex;gap:8px">
            <button id="checkoutBtn">Place Order</button>
            <button class="small" id="clearBtn">Clear</button>
          </div>

          <div id="orderResult" style="margin-top:12px"></div>
        </div>
      </aside>
    </div>

    <footer>
      <p class="note">Theme: Pastel pink & cream • Buttons must work • No extra demo items</p>
    </footer>
  </div>

  <script>
    // Product data (as requested)
    const PRODUCTS = [
      {id:'b1',name:'Idli & Dosa Batter',price:55,unit:'500g',desc:'Soft & tasty.'},
      {id:'b2',name:'Adai Batter',price:60,unit:'500g',desc:'Protein-rich.'},
      {id:'p1',name:'Idli Podi',price:55,unit:'100g',desc:'Spicy & fresh.'},
      {id:'p2',name:'Garlic Podi',price:50,unit:'100g',desc:'Bold garlic.'},
      {id:'o1',name:'Coconut Oil',price:180,unit:'1L',desc:'Pure.'},
      {id:'o2',name:'Groundnut Oil',price:160,unit:'1L',desc:'Nutty.'},
      {id:'o3',name:'Gingelly Oil',price:200,unit:'1L',desc:'Aromatic.'},
      {id:'c1',name:'Small Coconut',price:30,unit:'pc',desc:'Fresh.'},
      {id:'c2',name:'Medium Coconut',price:35,unit:'pc',desc:'Balanced.'},
      {id:'c3',name:'Big Coconut',price:40,unit:'pc',desc:'Juicy.'}
    ];

    const PACKAGING_FEE = 15;
    let cart = {};

    const productsEl = document.getElementById('products');
    const cartListEl = document.getElementById('cartList');
    const totalPriceEl = document.getElementById('totalPrice');
    const orderResultEl = document.getElementById('orderResult');

    function renderProducts(){
      productsEl.innerHTML = '';
      PRODUCTS.forEach(p => {
        const el = document.createElement('div');
        el.className = 'product';
        el.innerHTML = `
          <div class="img">${p.name.split(' ').slice(0,2).map(w=>w[0]).join('')}</div>
          <div class="meta">
            <h3>${p.name}</h3>
            <p class="muted">${p.unit} • ₹${p.price}</p>
            <p class="muted">${p.desc}</p>
          </div>
          <div class="controls">
            <div>
              <label class="muted">Qty</label>
              <input type="number" min="1" value="1" id="qty-${p.id}">
            </div>
            <div style="margin-top:8px">
              <button data-id="${p.id}" class="addBtn">Add to Cart</button>
            </div>
          </div>
        `;
        productsEl.appendChild(el);
      });
      document.querySelectorAll('.addBtn').forEach(b=>b.addEventListener('click',addToCart));
    }

    function addToCart(e){
      const id = e.currentTarget.dataset.id;
      const qtyEl = document.getElementById(`qty-${id}`);
      const qty = Math.max(1,parseInt(qtyEl.value)||1);
      const prod = PRODUCTS.find(x=>x.id===id);
      if(!prod) return;
      if(cart[id]) cart[id].qty += qty; else cart[id] = {...prod, qty};
      renderCart();
      showToast('Item added to cart 🛒');
    }

    function renderCart(){
      cartListEl.innerHTML = '';
      let subtotal = 0;
      Object.values(cart).forEach(item=>{
        const div = document.createElement('div');
        div.className='cart-item';
        const line = document.createElement('div');
        line.innerHTML = `${item.name} x ${item.qty} <span class="muted">(${item.unit})</span>`;
        const right = document.createElement('div');
        right.innerHTML = `₹${item.price*item.qty} <button class="small" data-id="${item.id}" style="margin-left:8px">Remove</button>`;
        div.appendChild(line);div.appendChild(right);
        cartListEl.appendChild(div);
        subtotal += item.price*item.qty;
      });
      const total = subtotal>0 ? subtotal + PACKAGING_FEE : 0;
      totalPriceEl.textContent = `₹${total}`;
      document.querySelectorAll('.cart-item .small').forEach(btn=>btn.addEventListener('click',removeItem));
    }

    function removeItem(e){
      const id = e.currentTarget.dataset.id;
      delete cart[id];
      renderCart();
    }

    document.getElementById('clearBtn').addEventListener('click',()=>{cart={};renderCart();orderResultEl.innerHTML='';});

    document.getElementById('checkoutBtn').addEventListener('click',()=>{
      if(Object.keys(cart).length===0){showToast('Cart is empty');return}
      const name = prompt('Enter your name','');
      if(!name){showToast('Order cancelled');return}
      const phone = prompt('Phone number','8072100476');
      if(!phone){showToast('Order cancelled');return}
      const address = prompt('Address','6th Sector, Morais City, Trichy');
      if(!address){showToast('Order cancelled');return}

      // build order summary
      let subtotal = 0;
      let itemsList = '';
      Object.values(cart).forEach(i=>{subtotal += i.price*i.qty; itemsList += `${i.name} x ${i.qty} — ₹${i.price*i.qty}\n`;});
      const total = subtotal + PACKAGING_FEE;
      const orderText = `Order Summary:\nName: ${name}\nPhone: ${phone}\nAddress: ${address}\n\nItems:\n${itemsList}\nPackaging: ₹${PACKAGING_FEE}\nTotal: ₹${total}`;

      // show result and success
      orderResultEl.innerHTML = `<div class="success">Order Placed Successfully ✅<br><pre style="white-space:pre-wrap">${orderText}</pre></div>`;

      // mock send to admin: open mailto (works if email client exists)
      const mailto = `mailto:sahidyack04@gmail.com?subject=New%20Order%20from%20Sai%20Products&body=${encodeURIComponent(orderText)}`;
      // open mail client in new tab
      window.open(mailto,'_blank');

      // clear cart
      cart = {};
      renderCart();
    });

    function showToast(msg){
      const s = document.createElement('div');
      s.className='success';
      s.style.position='fixed';s.style.right='18px';s.style.bottom='18px';s.style.zIndex=9999;
      s.textContent = msg;
      document.body.appendChild(s);
      setTimeout(()=>s.remove(),2200);
    }

    // initial
    renderProducts();
    renderCart();
  </script>
</body>
</html>
